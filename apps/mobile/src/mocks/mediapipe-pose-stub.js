// Stub for @mediapipe/pose — required by @tensorflow-models/pose-detection's
// BlazePose MediaPipe backend, which we don't use (we use MoveNet only).
module.exports = {
  Pose: class {},
  POSE_CONNECTIONS: [],
  POSE_LANDMARKS: {},
  VERSION: '0',
};
